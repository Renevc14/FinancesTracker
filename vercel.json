{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "regions": ["iad1"],
  "ignoreCommand": "if [ -n \"$VERCEL_GIT_COMMIT_REF\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ]; then exit 0; else exit 1; fi",
  "buildCommand": "npx drizzle-kit push --force && next build",
  "crons": [
    {
      "path": "/api/cron/daily",
      "schedule": "5 6 * * *"
    }
  ]
}
